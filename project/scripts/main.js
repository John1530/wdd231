const menuButton=document.querySelector("#menu-button");const nav=document.querySelector("#primary-nav");if(menuButton&&nav){menuButton.addEventListener("click",()=>{const open=menuButton.getAttribute("aria-expanded")==="true";menuButton.setAttribute("aria-expanded",String(!open));menuButton.setAttribute("aria-label",open?"Open navigation menu":"Close navigation menu");nav.classList.toggle("open",!open)});nav.querySelectorAll("a").forEach(link=>{if(link.pathname===location.pathname)link.setAttribute("aria-current","page")})}const year=document.querySelector("#current-year");const modified=document.querySelector("#last-modified");if(year)year.textContent=new Date().getFullYear();if(modified)modified.textContent=`Last modified: ${document.lastModified}`;const submitted=document.querySelector("#submitted-at");if(submitted)submitted.value=new Date().toISOString();