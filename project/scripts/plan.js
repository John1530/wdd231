const params=new URLSearchParams(location.search);const details=document.querySelector("#form-details");const recommendation=document.querySelector("#recommendation");const fields=[['name','Name'],['email','Email'],['interest','Interest'],['experience','Experience'],['goal','Goal'],['submitted','Submitted']];details.innerHTML=fields.map(([key,label])=>`<dt>${label}</dt><dd>${params.get(key)||"Not provided"}</dd>`).join("");const interest=params.get("interest")||"aerospace fundamentals";const suggestions={Aerodynamics:"Begin with the four forces and lift calculator, then study airfoils and CFD.",Propulsion:"Start with thrust, engine categories, energy conversion, and nozzle fundamentals.","Aircraft Design":"Study requirements, configuration choices, weight balance, and multidisciplinary trade-offs.","Space Systems":"Begin with orbital motion, launch vehicles, spacecraft subsystems, and mission design.","Flight Control and AI":"Build foundations in flight dynamics, feedback control, sensors, Python, and machine learning."};recommendation.innerHTML=`<h3>Recommended starting point</h3><p>${suggestions[interest]||"Begin with the Flight Concepts page and explore aircraft that match your interest."}</p>`;